Literal    : INT FLOAT STRING
Operator   : PLUS MINUS TIMES DIVBY POW SQRT NOT
Comparison : EQUALSEQ NOTEQ LSSTHN GRTTHN LSSTHNEQ GRTTHNEQ
Combiner   : AND XOR OR



notraceexpr:
  - KEYWORD:var IDENTIFIER EQUALS expr
  - IDENTIFIER EQUALS expr
  - expr

expr:
  - opexpr

opexpr:
  - factor (factor (Operator|Comparison|Combiner) | opexpr)*

factor:
  - (PLUS|MINUS) factor
  - LPAREN expr RPAREN
  - ifexpr
  - forexpr
  - whileexpr
  - atom

ifexpr:
  - KEYWORD:if LPAREN expr RPAREN codeblock
    (KEYWORD:elif LPAREN expr RPAREN codeblock)*
    (KEYWORD:else codeblock)?

forexpr:
  - KEYWORD:for IDENTIFIER KEYWORD:in LPAREN expr RPAREN codeblock

whileexpr:
  - KEYWORD:while LPAREN expr RPAREN codeblock

atom:
  - Literal
  - IDENTIFIER
  - arrayexpr

arrayexpr:
  - LSQUARE EOL* (expr
    (COMMA EOL* expr)*)? COMMA? EOL*
    RSQUARE



codeblock:
  - LCURLY EOL*
    (expr EOL*)*
    RCURLY
